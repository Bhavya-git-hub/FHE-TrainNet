# FHE-TrainNet dependencies.
#
# TenSEAL is pinned exactly: it is the cryptographic dependency, every capability
# claim in this project was probed against this build, and docs/capabilities.json
# records what that probe found. Changing it means re-running
# scripts/probe_library.py and checking the claims still hold.
#
# The rest are floors rather than exact pins, deliberately. This app is deployed
# to hosted Python environments whose interpreter version is not ours to choose,
# and exact pins on fast-moving scientific packages resolve on one Python and
# fail on the next. The floors are the versions the project was developed and
# tested against.

tenseal==0.3.18          # CKKS via Microsoft SEAL. The only cryptographic dependency.

numpy>=1.26
scikit-learn>=1.3        # dataset sources and metrics only; never the encrypted maths
pandas>=2.0
plotly>=5.18             # dashboard charts
matplotlib>=3.8          # static figures written to results/ and reports/
streamlit>=1.31          # local demonstration dashboard
psutil>=5.9              # CPU / memory measurement
PyYAML>=6.0              # experiment configuration files
pytest>=8.0
